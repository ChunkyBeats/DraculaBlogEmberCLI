{{#if isEditing}}
  {{partial 'post/edit'}}
  <button {{action 'doneEditing'}}>Done</button>
{{else}}
  <h1>{{title}}</h1>

  <h2>by {{author.name}}</h2>

  <hr>

  <div class="body">
    {{body}}
  </div>
  <br>
  <button {{action 'edit'}}>Edit</button>
{{/if}}
